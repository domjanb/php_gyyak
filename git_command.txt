git init


git status

git add git_command.txt

git commit -m "git parancsok 1"

'hash file tipusa (hash elsõ 4 karaktere kell/hash az objectben van)
git cat-file e2e3 -t 

'hash file tipusa (hash elsõ 4 karaktere kell)
git cat-file e2e3 -p



az aktuális módosítás gyökere
git rev-parse HEAD

VAGY 
git cat-file -p HEAD



git log 

lognak csak az érdemi része
git log 9019 --oneline



VISSZAVONNI AZ UTOLSÓ MÓDOSÍTÁST
git reset --soft HEAD^


VISSZAVONNI AZ UTOLSÓ 2DB MÓDOSÍTÁST
git reset --soft HEAD^^
vagy
git reset --soft HEAD~2



hozzaadom az összes txt-t
git add *.txt

git commit -m "git parancsok tobb file hozza"


a gitben az emailnév amivel kapcsolódik
git config --global user.email

a gitben az emailnév átállítása
git config --global user.email "emilcim"


a gitben az usernév amivel kapcsolódik
git config --global user.name

a gitben az usernév átállítása
git config --global user.name "neve"


githubhoz hozzákötés
git remote add origin https://github.com/domjanb/php_gyyak.git

feltöltés a hubra (master branch)
git push origin master -u

a -u azt jelenti, hogy megjegyzi az origin master-t igy késõbb , már csa
git push


letöltés a hubról (master branch)
git pull origin master 


*elso gitbelepesnel/ha nem mûködik a pull
git pull origin master --allow-unrelated-histories


ha még nincs reponk a gépen:
git clone https://github.com/domjanb/php_gyyak.git